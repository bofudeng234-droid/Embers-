# embers 可扩展性架构

> 决策记录。核心原则:**追踪能力和减压/重算作用在不同数据层,永不冲突。**

## 一、两层数据(最重要的认知)

| 层 | 是什么 | 用途 | 能不能删/重算 | 谁负责"找回视频" |
|---|---|---|---|---|
| **检索层** | `vec_videos`(2048d) / `vec_anchors` / `vec_images` + `videos` 表 | KNN 检索 | **永不删** | ✅ 靠它(video_id ↔ 向量) |
| **可视化层** | `star_coords(video_id, x, y, z, galaxy_id)` | 3D 星空显示 | ✅ 可随时全量重算覆盖 | ❌ 只负责好看 |

类比:图书馆。检索向量 = 每本书的"内容指纹卡"(永不撕);星空坐标 = 大厅墙上的"馆藏地图"(随时能重画)。重画地图不动书。

**推论**:删向量 = 真的丢视频(再也搜不到)。所以减压**绝不靠删向量**。星空全量重算只覆盖 `star_coords`,检索层零影响。

## 二、规模演进路线(每个坎的触发点 + 动作)

| 规模 | 检索延迟 | 存储 | 动作 | 现在做? |
|---|---|---|---|---|
| **< 1 万** | 暴力 KNN <50ms | ~1GB | 什么都不做(过早优化是浪费)| — |
| **~10 万** | 暴力 KNN 数百 ms | ~10GB | 加 **HNSW 近似索引**(O(N)→O(logN))| ⏳ 到坎再做 |
| **~100 万** | HNSW 仍 ms 级 | ~100GB | 向量 **量化**(float32→int8/PQ,体积 ÷4~÷30,精度略降)| ⏳ 到坎再做 |

全程**不删向量**。减检索压力靠 HNSW,减存储靠量化(压缩的还是那条向量,video_id 不变,追踪不变)。

## 三、星系聚类:可视化与检索加速复用同一套

对所有向量聚类(HDBSCAN/KMeans)→ N 个语义星系,**一次聚类两个用途**:

- **可视化**:3D 星空里每个星系一片区域 + 固定色调(美食=暖橙,AI=冷蓝…)
- **检索加速(IVF 思想)**:query 先定位最近的 2-3 个星系中心,只在这些星系内精检索,跳过无关星系

星系只是给向量打 `galaxy_id` 标签,不碰向量本身,追踪不变。**到 10 万级时,星系 IVF 与 HNSW 二选一或叠加。**

## 四、星空全量重算的权衡

每月全量重跑 UMAP + 聚类 → 覆盖 `star_coords`(语义最准)。代价:星星位置会变。缓解:

1. **过渡动画**:老星星从旧位置平滑飞到新位置(几秒)
2. **星系固定色调当路标**:位置变但颜色/星系名不变,用户靠区域认路

## 五、现在做 vs 到坎再做(明确分界)

**现在做**(数据 ≈ 几百~几千条,正合适):
- `pipeline/starfield.py`:UMAP 2048d→3D + HDBSCAN 星系 → 写 `star_coords`
- `/starfield` 端点 + Three.js 3D 星空前端 + 检索点亮动画
- 全量重算挂进 daemon(每月一次)或手动触发

**到坎再做**(别提前):
- HNSW 索引 — 触发条件:检索 >300ms 或视频 >5 万
- 向量量化 — 触发条件:DB >50GB 或视频 >50 万
- 星系 IVF 粗排 — 触发条件:同 HNSW,或星系数 >50

---
_决策时间 2026-05-15 · 详见 git log v0.9 之后的 starfield 相关 commit_
